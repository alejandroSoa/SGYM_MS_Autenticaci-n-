<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Restablecer Contraseña</title>
  <style>
    body {
      background: url('/images/gym-bg.jpg') no-repeat center center fixed;
      background-size: cover;
      font-family: 'Arial', sans-serif;
      position: relative;
      min-height: 100vh;
      margin: 0;
    }

    body::before {
      content: "";
      position: absolute;
      inset: 0;
      background-color: rgba(80, 0, 150, 0.6);
      z-index: -1;
    }

    .auth-container {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 1rem;
    }

    .auth-box {
      width: 100%;
      max-width: 400px;
      background: white;
      border-radius: 1rem;
      padding: 2rem;
      text-align: center;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
      z-index: 1;
    }

    h1 {
      color: #6a0dad;
      margin-bottom: 1.5rem;
    }

    .auth-form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .auth-form input {
      padding: 0.75rem;
      border: 1px solid #ccc;
      border-radius: 1rem;
      font-size: 1rem;
    }

    .auth-form button {
      background-color: #6a0dad;
      color: white;
      border: none;
      border-radius: 1rem;
      padding: 0.75rem;
      cursor: pointer;
      font-weight: bold;
      font-size: 1rem;
      transition: background-color 0.3s ease;
    }

    .auth-form button:hover {
      background-color: #530f9b;
    }

    .back-link {
      margin-top: 1rem;
      display: inline-block;
      color: #6a0dad;
      text-decoration: none;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="auth-container">
    <div class="auth-box">
      <h1>Restablecer Contraseña</h1>

      <form method="POST" action="/oauth/resetpassword" class="auth-form">
        @csrf
        <input type="email" name="email" placeholder="Correo electrónico" required />
        <input type="text" name="token" placeholder="Código de verificación" required />
        <input type="password" name="password" placeholder="Nueva contraseña" required />
        <input type="password" name="password_confirmation" placeholder="Confirmar nueva contraseña" required />
        <input type="hidden" name="redirect_uri" value="{{ redirectUri }}" />
        <button type="submit">Restablecer</button>
      </form>

      <a href="/oauth/login" class="back-link">Volver al inicio de sesión</a>
    </div>
  </div>
</body>
</html>
